<form method="post" action="{{ path('modele_year_delete', {'id': modele_year.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modele_year.id) }}">
    <button class="btn">Delete</button>
</form>
